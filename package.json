{
  "name": "ci-failure-explainer",
  "private": true,
  "scripts": {
    "build": "ncc build index.js -o dist"
  },
  "type": "commonjs",
  "version": "0.0.1",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "adm-zip": "^0.5.16"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.4"
  }
}
